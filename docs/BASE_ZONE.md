# Зоны базы (BaseZone)

Зона базы — прямоугольный объём (AABB в блоках, **с ограничением по высоте Y**),
привязанный к команде-владельцу. Игрок вражеской команды, попавший в чужую зону,
получает полноэкранное предупреждение с обратным отсчётом и погибает, если не
покинул зону вовремя.

## Поведение

- **Свои** (команда-владелец) входят свободно и **неуязвимы для урона от любых игроков**
  внутри своей зоны: ни союзник (тимкилл), ни враг (обстрел базы снаружи) их не достанут.
  Отменяется только урон, инициированный игроком, — мобы, падение, окружение и урон
  самому себе действуют как обычно. Стрелять из зоны наружу свои могут: урон, который
  наносит защитник, не затрагивается.
- **Враги** (любая другая команда, включая игроков без команды) при входе видят
  титр «⚠ ЧУЖАЯ БАЗА» + подзаголовок с отсчётом. Отсчёт по умолчанию 5 секунд.
- Отсчёт **сбрасывается при выходе** из зоны; повторный вход — с начала.
- По истечении отсчёта — смерть от кастомного урона (`pjmbasemod:base_zone`),
  сообщение «… погиб в зоне вражеской базы».
- **OP** (`permission level ≥ 2`), а также **creative/spectator** игнорируют
  защиту (админ-осмотр).

## Команды (`/pjm basezone`, требует OP level 2)

- `create <name> [displayName]` — создать зону.
- `delete <name>` — удалить.
- `list` / `info <name>` — список / детали.
- `pos1 <name>` / `pos2 <name>` — задать угол зоны по текущей block-позиции игрока
  (включая Y) и его размерности.
- `owner <name> <team>` — назначить команду-владельца (id из `teams.definitions`).
- `displayname <name> <displayName>` — сменить отображаемое имя.

Зона активна (защищает), когда заданы обе точки pos1/pos2 **и** назначен владелец.

## Конфиг (секция `baseZone`)

- `enabled` (bool, дефолт `true`) — вкл/выкл систему.
- `countdownSeconds` (int, дефолт `5`, диапазон 1..60) — время до смерти.
- `blockExplosions` (bool, дефолт `true`) — отключать урон от взрывов SuperbWarfare и
  гранат WarBornExplosives любому игроку внутри зоны базы (защита от закидывания
  взрывчаткой). Ловится по тегу `IS_EXPLOSION`, SBW-типам `vehicle_explosion`/`mine`
  и любому урону от сущностей мода `warbornexplosives` (осколки/граната).

## Реализация

- `common/basezone/BaseZone` — данные зоны (AABB + owner), NBT.
- `common/basezone/BaseZoneSavedData` — персист (`pjmbasemod_basezones`) + `findZoneAt`.
- `common/basezone/BaseZoneManager` — enforcement в `PlayerTickEvent.Post`
  (`PjmServerEvents.onPlayerTick`): титры, отсчёт, смерть. Состояние отсчёта —
  рантайм-`Map`, не персистится. Неуязвимость защитников от урона игроков
  (`shouldCancelPlayerDamage`) и отмена урона от взрывов (`shouldCancelExplosion`) —
  в `LivingIncomingDamageEvent` (`PjmServerEvents.onIncomingDamage`).
- `data/pjmbasemod/damage_type/base_zone.json` — кастомный источник урона.
